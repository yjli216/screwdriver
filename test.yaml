workflow:
        - publish

shared:
        image: buildpack-deps 

jobs:
        main:
                steps:
                        - export: echo hello 
        
        publish:
                steps:
                        - test: echo world

